{
  "name": "nottrello",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:server",
    "setup:app": "npm run setup:server && npm run setup:client",
    "setup:client": "cd NotTrello.client && npm i",
    "setup:server": "cd NotTrello && npm i",
    "start": "node NotTrello/index.js"
  },
  "engines": {
    "node": "^14.17.0"
  }
}
